# Rate limiting zones (http context; conf.d/*.conf is included inside http {})
limit_req_zone $binary_remote_addr zone=general:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=api:10m rate=5r/s;
limit_conn_zone $binary_remote_addr zone=addr:10m;

server {
    listen 80;
    server_name pixup.id www.pixup.id;

    # Redirect HTTP to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name pixup.id www.pixup.id;

    # SSL Certificates
    ssl_certificate /etc/letsencrypt/live/pixup.id/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/pixup.id/privkey.pem;

    # SSL Configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # Limit concurrent connections per client IP
    limit_conn addr 20;

    # Frontend
    location / {
        # Rate limit: 10 req/s per IP, short burst allowed
        limit_req zone=general burst=20 nodelay;

        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Backend API
    location /api/ {
        # Rate limit: 5 req/s per IP for the (expensive) API
        limit_req zone=api burst=10 nodelay;

        proxy_pass http://backend:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts for long processing
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;

        # Max upload size (app enforces 1.5MB; 2M leaves margin for multipart overhead)
        client_max_body_size 2M;
    }
}
